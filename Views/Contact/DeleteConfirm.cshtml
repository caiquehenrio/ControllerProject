@model ContactModel
@{
    ViewData["Title"] = "Delete Confirm";
}

<div class="text-center">
    <h3>Do you really want to delete the contact (@Model.Name)?</h3>
</div>

<a class="btn btn-light" role="button" asp-controller="Contact" asp-action="Index">Cancel</a>
<a class="btn btn-light" role="button" asp-controller="Contact" asp-route-id="@Model.Id" asp-action="Delete">Confirm</a>
